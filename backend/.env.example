# 🌐 Cloud Environment Variables
# Copy this to .env and fill your values

PORT=5000

# PostgreSQL (Render / Railway / AWS RDS)
DB_HOST=your-db-host.render.com
DB_PORT=5432
DB_NAME=inventory_db
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JWT
JWT_SECRET=change_this_to_random_string_32_chars_min
JWT_EXPIRE=7d

# CORS (Frontend URL - comma separated for multiple origins)
CORS_ORIGIN=https://your-frontend-url.vercel.app

# Optional: set to 'production' on live servers to hide error details
NODE_ENV=production
